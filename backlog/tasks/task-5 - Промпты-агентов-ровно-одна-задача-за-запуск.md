---
id: TASK-5
title: 'Промпты агентов: ровно одна задача за запуск'
status: Done
assignee:
  - '@developer'
created_date: '2026-09-03 15:18'
updated_date: '2026-09-03 15:27'
labels: []
dependencies: []
type: docs
ordinal: 5000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Обновить .agents/<имя>.md (в первую очередь developer.md): при запуске агент берёт ровно одну назначенную на него задачу, выполняет её по протоколу backlog (task-execution → проверка AC → task-finalization) и завершается. Следующую задачу берёт следующий запуск (или цикл). Без назначенных задач агент не выдумывает работу и сам задачи не создаёт.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 developer.md содержит явное правило: одна задача за один запуск, затем остановиться
- [x] #2 Указан протокол: найти назначенную задачу → In Progress → выполнить → проверить AC → final summary → Done
- [x] #3 Агент не создаёт задачи сам и не берёт работу без назначенных ему задач
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Переписать .agents/developer.md: явное правило «ровно одна задача за запуск, затем остановиться».
2. Описать протокол: найти назначенную задачу → In Progress → выполнить → проверить AC → final summary → Done (со ссылками на backlog instructions).
3. Добавить запрет: агент не создаёт задачи сам и не берёт работу без назначенных задач.
4. Сохранить существующую логику выбора (приоритет → порядок; заблокированная → взять блокирующую).
5. Проверить acceptance criteria и финализировать TASK-5.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Переписал .agents/developer.md: добавлены разделы «Главное правило: ровно одна задача за запуск», «Выбор задачи», «Протокол выполнения задачи», «Запреты». Сохранена логика выбора по приоритету/порядку и правила блокировки. Других .agents/*.md пока нет — обновлён только developer.md.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Переписан .agents/developer.md: добавлены разделы «Главное правило: ровно одна задача за запуск» (AC1 — явное правило «ровно одна назначенная задача, затем остановиться»), «Выбор задачи» (сохранена логика приоритет → порядок, при блокировке — взять блокирующую задачу) и «Протокол выполнения задачи» (AC2 — task-execution → In Progress → выполнение → проверка AC → final summary → Done). Добавлен раздел «Запреты» (AC3 — не создавать задачи сам, не брать работу без назначения). Других .agents/*.md в проекте нет. Проверено grep-паттернами по содержимому файла.
<!-- SECTION:FINAL_SUMMARY:END -->
